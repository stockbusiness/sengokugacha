# PR5 着手前の確認依頼（送付用）

- 作成日: 2026-08-22
- 対象: 「千ノ国パスポート × OVEW Wallet 次工程実装指示書」（2026-08-22版）
- 目的: 指示書と既存の確定事項との食い違いを、着手前に解消する

宛先が異なるため2通に分けています。**【1】は OVEW Wallet 担当者、【2】は指示書作成者（運営側）にしか判断できません。** 各章をそのままコピーしてご使用ください。

> **秘密値（APIキー・署名鍵・アクセストークン・本番URLの秘密部分）は、いずれの返信にも記載しないでください。** 必要なのは方式・有無・時期だけです。

---

# 【1】OVEW Wallet 担当者宛

**件名: 接続仕様の再確認（8/20 回答と 8/22 指示書の食い違いについて）**

お世話になっております。千ノ国パスポート側の実装担当です。

8月22日付「千ノ国パスポート × OVEW Wallet 次工程実装指示書」を受領し、PR5（Wallet 送信）の準備に入ろうとしています。その前に、**8月20日にご回答いただいた内容と指示書の記載が食い違っている点**が2つあり、どちらを正とすべきか判断できないため確認させてください。

古い前提のまま実装すると作り直しになるため、着手前に確定させたい、という趣旨です。

## 確認1: 利用者識別子（指示書 A-1）

| 出典 | 内容 |
|---|---|
| 8/20 のご回答 | ユーザー解決は `service_code` + `external_user_id`。**`common_user_id` は受け付けない** |
| 8/22 の指示書 | Wallet は「`common_user_id` 起点の残高照会基盤」まで実装済み。A-1 は **`common_user_id` を第一候補** |

- 現在の Wallet 実装は、**付与API**で `common_user_id` を受け付けますか（残高照会だけでなく付与も、という点が重要です）
- 受け付ける場合、`service_code` + `external_user_id` 方式は**併存**しますか。それとも**置き換え**ですか
- 併存する場合、両方を送ったときの**優先順位**はどちらですか

**回答欄**

```
（ここに記入）
```

## 確認2: 照合手段（指示書 A-5）

| 出典 | 内容 |
|---|---|
| 8/20 のご回答 | **取引履歴取得APIは存在しない** |
| 8/22 の指示書 | A-5 で「取引照会API、日次CSV、管理画面照合のいずれかを確定」。4.2 に取引照会またはCSVの用意が Wallet 側タスクとして記載 |

- 取引照会API または CSV 出力は、**現時点で実装済みですか、これから追加ですか**
- これから追加の場合、**提供可能な時期の見込み**をお知らせください
- 抽出条件として、**期間・`service_code`・`rule_code`** の3つを指定できますか

**回答欄**

```
（ここに記入）
```

## お願い

今後の行き違いを防ぐため、ご回答には **「どの時点の実装を指すか（日付またはコミット）」** を添えていただけると助かります。前回は、こちらが参照した文書の記載が実際の稼働状態と異なっていたことが後から判明しました。

**秘密値はこの返信に記載しないでください。** 値そのものは、確定後に Vercel の環境変数または管理設定へ直接投入する形でお願いします。

## 参考: この2点が決まらない間、こちらで進める範囲

| | 内容 |
|---|---|
| **進める** | 契約型、状態機械、Fake アダプタ、冪等性キー、claim/fencing（**外部送信なし**） |
| **止める** | HTTP アダプタの実装、実送信の有効化、Feature Flag の ON |

指示書 §3 の「優先度A未回答時＝実送信停止／Fakeのみ」に沿った運用です。

---

# 【2】指示書作成者（運営側）宛

**件名: 8/22 指示書について、着手前に確認したい4点**

お世話になっております。8月22日付「千ノ国パスポート × OVEW Wallet 次工程実装指示書」を確認しました。準備段階（PR5-a）には着手できますが、その前に判断をお願いしたい点が4つあります。

## 確認1: `common_user_id` が本番で全ユーザー未解決です（A-1 の実行可能性）

定期照合が次を検知しています。

```
common_user_id 未解決: 19件
解決試行の滞留: 9件
```

**総ユーザー数が19名なので、例外なく全員が未解決**です。

A-1 で `common_user_id` を採用する場合、**現時点では誰にも付与できません**。既存コードも「共通IDを解決できていない場合は付与を行わない」という設計です。

これは Passport 側の不具合ではなく、**Agency との接続がまだ無いことの帰結**と見ています。先の C7 では「Agency の接続仕様・認証情報が確定後に19名の解決を実施する。現在は自動変更や推測による紐付けを行わない」とご指示いただきました。

つまり **A-1 の実行可能性は Agency 連携の完了に依存します**。指示書 §8 の Go 条件にこの依存が明記されていないため、**「`common_user_id` の解決完了」を Go 条件に追加してよいか**をご確認ください。

**回答欄**

```
（追加する ／ 追加しない ／ その他）
```

## 確認2: 5システム実施順序書との順序

`00_5SYSTEM_EXECUTION_ORDER` §3 は「**8. 上記が安定してから**『はじまりの旅』と段階的3,000 OVE付与を実装する」としており、5システム横断E2Eの完了が前提でした。今回の指示書は、それより前に PR5 へ着手する内容です。

`01_PASSPORT` の変更禁止範囲（「はじまりの旅を安全化PRへ同梱すること」）には**抵触しません**。別PR系列だからです。ただし**実施順序書を上書きするものかどうか**を明示していただきたく思います。

**回答欄**

```
（実施順序書を上書きする ／ 準備工程のみ先行し実送信は順序どおり ／ その他）
```

## 確認3: 状態値 `CANCELLED` の扱い

現行の `learning_journey_reward_requests.status` は CHECK 制約で7状態です。

```
PENDING / PROCESSING / SUCCEEDED / FAILED / LIMIT_HELD / CANCELLED / REVERSED
```

指示書 §5.2 が求める **`REWARD_DISABLED`** と **`DEFERRED_DECISION`** は現行にありません（PR5-a で追加します）。一方、**`CANCELLED` は指示書の一覧に登場しません**。

- **廃止**するのか、単に §5.2 で省略されただけか
- 廃止する場合、既存レコードの扱い（現在は0件のはずです）

**回答欄**

```
（存続 ／ 廃止 ／ その他）
```

## 確認4: PR-P1 系列との優先順位

現在の状況です。

| 項目 | 状態 |
|---|---|
| PR #170（PR-P1c 販売事実Outbox + C4） | draft・CI 9/9 green。**C6 の解決待ちでマージ保留** |
| C6（cron の1本だけ実行記録がない件） | 調査結果を提出済み。**確認待ち** |

PR-P1c が C6 待ちで止まっているため、**その間に PR5-a を並行して進めることは可能**です。どちらを優先するかご指示ください。

なお **C6 は PR5 にも関係します。** 指示書 §5.1 は「既存 outbox の実績ある仕組みを再利用する」としていますが、その仕組みの一部が動いていない疑いが未解消です。**PR5-a の前に決着させるほうが安全**だと考えます。

**回答欄**

```
（PR5-a を先行 ／ C6 解決を優先 ／ 並行 ／ その他）
```

---

**ご指示をいただければ、PR5-a の実装計画（外部送信なし・Fake アダプタまで）から作成します。** 指示書 §9 の提出物一覧と §10 の完了報告テンプレートにも沿わせます。

---

# 付録: 食い違いの一覧（送付不要・社内整理用）

| # | 論点 | 8/22 指示書 | 既存の確定事項 | 判断者 |
|---|---|---|---|---|
| 1 | 利用者識別子 | `common_user_id` 第一候補 | 8/20 Wallet 回答: 受け付けない | Wallet |
| 2 | 照合手段 | 取引照会API/CSV を前提 | 8/20 Wallet 回答: 履歴APIは存在しない | Wallet |
| 3 | `common_user_id` の解決状況 | 言及なし | **本番19名全員が未解決**（C7） | 運営 |
| 4 | 実施順序 | PR5 へ着手 | 5システム順序書 §3-8: 最後 | 運営 |
| 5 | 状態値 | `REWARD_DISABLED` / `DEFERRED_DECISION` を追加、`CANCELLED` の記載なし | 現行7状態に `CANCELLED` あり | 運営 |
| 6 | outbox 基盤 | 「実績ある仕組みを再利用」 | C6 未解決（cron 1本の記録なし） | 運営 |

**抵触しない点（確認済み）**

- `01_PASSPORT` の変更禁止範囲「はじまりの旅や3,000 OVE段階付与を安全化PRへ同梱すること」→ 別PR系列のため抵触しない
- 指示書の「付与機能はOFFを維持」→ Q8 の凍結方針（4フラグとも `false`、設定行0件）と一致する
- `external_user_id` は既に作成時点で確定・以後書き換えない構造になっている
