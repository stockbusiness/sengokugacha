# C4 文言修正計画 — 売上ログの「未払い売上合計」

- 作成日: 2026-08-21
- 対象: `stockbusiness/sengokugacha`（Passport）
- 準拠: C4 回答（案b・文言を調整する）
- **本計画の承認を受けるまで、コード変更は開始しません。**

## 1. 目的

`/admin/agent-sales`（売上ログ）の見出し「代理店別 未払い売上合計」が、**未払いの「報酬」**と誤解されうるため文言を変えます。

**表示文言だけの変更です。集計ロジック・対象範囲・データは一切変更しません。**

## 2. 変更内容

`src/app/admin/(dashboard)/agent-sales/page.tsx` の1箇所のみ。

**変更前**

```
代理店別 未払い売上合計
```

**変更後**（ご指定の推奨文言）

```
代理店別 売上合計（支払記録未作成分）
```

補足文をその直下に追加します。

```
表示金額は売上総額です。報酬額ではありません。報酬計算はこの画面では行いません。
```

## 3. 変更しないもの

| 対象 | 理由 |
|---|---|
| `unpaidTotalsByAgent` の集計ロジック | ご指示「集計ロジックや対象範囲は変更しない」 |
| 抽出条件・対象範囲 | 同上 |
| 既存の注記文 | 「Phase1は記録のみです。（中略）金額は売上総額であり、実際の報酬額(手数料率反映後)の計算は別途手動で行ってください。」は既に正しいため、そのまま残します |
| 一覧表示・CSV出力 | 触りません |
| 変数名・関数名 | 表示文言のみの変更に留めます。`unpaidTotalsByAgent` という変数名は残りますが、これはコード内部の名前であり利用者には見えません |

> 変数名まで直すと差分が広がり、「表示文言だけの変更」という性質が見えにくくなります。名前も揃えたい場合は別途ご指示ください。

## 4. テスト

| # | 内容 |
|---|---|
| 1 | 「未払い売上」という表記が売上ログ画面に存在しないことをソース走査で確認 |
| 2 | 「報酬額ではありません」の注記が存在することを確認 |
| 3 | `tsc` / `lint` / `test:unit` / `test:architecture` / `build` の非回帰 |

1・2 は PR-P1b で追加した `commission-write-guards.test.ts` の「売上ログ画面」セクションに足します。同セクションには既に「移管の仕組みを持ち込まない」テストがあり、置き場所として自然です。

## 5. 影響範囲

| 領域 | 影響 |
|---|---|
| 利用者 | なし（管理画面のみ） |
| 管理者 | 見出しの表記が変わり、注記が1行増えます。**数値・件数・並び順は変わりません** |
| データ | **変更なし** |
| DB | **変更なし。マイグレーションなし** |

## 6. ロールバック

コードの revert のみ。データ変更が無いため復旧作業は不要です。

## 7. PRの扱い

差分がごく小さいため、**単独のPRにするか、次のPR（PR-P1c 等）へ同梱するかをご指示ください。**

実行ルール §4「1つのPRに異なる目的を混在させない」に従えば単独PRが正しいと考えますが、1行の文言修正で1PRを起こすことの是非はご判断ください。**既定では単独PRとして出します。**
