# 外部購入管理 テスト報告書

「戦国パスポート開発者向け実装指示書v1.0」17章・19-2に対応する検証記録。

## 実施環境・実施日・実施者

| 項目 | 内容 |
|---|---|
| 実施環境 | (未実施。本番Supabase・Vercel環境での実施が必要) |
| 実施日 | - |
| 実施者 | - |

**重要**: このリポジトリの開発サンドボックスからは、ネットワークポリシーにより
本番Supabaseへ直接接続できない。加えて本リポジトリにはDBモック基盤・
統合テスト実行基盤が無いため(現状監査9章参照)、以下のケース1〜14は
**開発時点では未実施**であり、本番相当の環境で手動実行することを前提とした
手順書としてまとめている。実施者は各ケースを実行し、実施日・実際の結果・
未解決事項を追記すること。

自動テストで担保できている範囲は「単体テスト」節を参照。

---

## 単体テスト(自動、実施済み)

`npx vitest run` で以下が自動検証される(56テスト、全て合格)。

| ファイル | 検証内容 |
|---|---|
| `src/lib/external-order-state.test.ts` | 注文状態遷移マトリクスの妥当性(18ケース): 正常経路、部分割当からの遷移、キャンセル系遷移、終端状態からの遷移拒否、operator/manager権限判定 |
| `src/lib/external-orders.test.ts` | `computeOrderAssignmentStatus()`(割当状況判定の純粋関数、6ケース) |

DBに依存する処理(重複登録防止・二重割当防止・排他制御・トランザクション相当
処理)は単体テストでは検証できないため、以下の統合テストケースで確認する。

---

## 統合テスト(手動、指示書17-2準拠)

各ケースについて、実施日・実際の結果・判定(合格/不合格/未実施)を記入する。

### ケース1: 1注文・1区画

```text
外部注文登録 → 入金確認 → LINEユーザー紐付け → 1区画割当 → 権利付与 → /my-land表示
```

| 手順 | 期待結果 |
|---|---|
| `/admin/external-orders/new`で1区画分の注文を登録 | draft状態で作成される |
| 「入金待ちにする」 | `payment_pending`になる |
| 「入金確認を確定する」(本部管理者) | `payment_confirmed`を経て`user_link_pending`になる |
| 購入者のLINEユーザーを検索・紐付け(本部管理者) | `plot_assignment_pending`になる |
| 区画を1つ割当 | `ready_to_grant`になる |
| 「区画権利を付与する」(本部管理者) | `rights_granted`になり、区画が`sold`に変わる |
| 購入者アカウントで`/my-land`を開く | 区画が表示される |

実施日: ___ / 結果: ___ / 判定: 未実施

### ケース2: 1注文・複数区画

```text
3区画購入 → 3区画割当 → 3権利付与 → /my-landに3件表示
```

3区画分の明細を持つ注文を作成し、3区画すべてを割当てるまで`ready_to_grant`に
ならないこと、権利付与後に`/my-land`に3件表示されることを確認する。

実施日: ___ / 結果: ___ / 判定: 未実施

### ケース3: 一部割当

```text
3区画購入 → 2区画割当 → 未割当1件を明示 → 全割当前は権利確定不可
```

2区画のみ割当てた状態で`partially_assigned`になること、「区画権利を付与する」
ボタンが(ready_to_grantでないため)表示されないこと、`grant-rights`APIを
直接叩いても「すべての注文明細に必要数の区画が割り当てられていません」で
拒否されることを確認する(**部分確定は許可しない設計**。実装計画10章
仕様確認事項1参照)。

実施日: ___ / 結果: ___ / 判定: 未実施

### ケース4: LINE未登録

```text
注文登録 → ユーザー紐付け待ち → 権利付与不可 → 後日LINEユーザーと紐付け → 権利付与
```

紐付け前に区画割当・権利付与APIを直接叩いても、状態遷移マトリクス上
到達できない(`plot_assignment_pending`より先に進めない)ことを確認する。

実施日: ___ / 結果: ___ / 判定: 未実施

### ケース5: 注文ID重複

同一`external_shop_name`・`external_order_id`で2回登録を試み、2回目が
「同一ショップ・同一注文IDの注文がすでに登録されています」(409)で拒否
されることを確認する(`external_orders`のユニーク制約が実際に効いているかの
確認)。

実施日: ___ / 結果: ___ / 判定: 未実施

### ケース6: 区画二重割当

2つのブラウザタブ(または2人の管理者)から、同じ注文明細に同じ区画を
ほぼ同時に割当てようとし、片方のみ成功し、もう片方が
「この区画は現在割り当てできません」(409)になることを確認する
(`external_order_plot_assignments`の部分ユニークインデックスが実際に
競合を防いでいるかの確認)。

実施日: ___ / 結果: ___ / 判定: 未実施

### ケース7: 権利付与の二重送信

「区画権利を付与する」ボタンを二重クリック、またはAPIを連続で2回叩き、
2回目が成功してもエラーになっても、区画権利(`castle_plots.status='sold'`)が
1件分しか作成されない(2回分の権利が付与されない)ことを確認する
(`grantExternalOrderRights()`のガード付きupdateが再試行安全であることの確認)。

実施日: ___ / 結果: ___ / 判定: 未実施

### ケース8: 取消

```text
権利付与済み → 外部返金確認 → 権利取消 → /my-land状態更新 → 区画再販売可能
```

権利付与済みの注文を「返金済みにする」で取消し、区画が`available`に戻り
`/my-land`から消えること、購入者へLINE通知(返金反映)が送信されることを
確認する。

実施日: ___ / 結果: ___ / 判定: 未実施

### ケース9: 一部取消

```text
3区画購入・全区画権利付与済み → 1区画のみ取消 → 該当区画のみ再販売可能に戻る
                             → 残り2区画は権利付与済みのまま維持される
```

3区画の注文で権利付与まで完了させた後、1明細だけ「この明細を取消」を実行し、
その区画のみ`available`に戻って`/my-land`から消えること、他の2区画は
`sold`のまま`/my-land`に残ることを確認する。さらに、残り2明細も順番に
取消していき、最後の1明細を取消した時点で注文全体が自動的に
`cancelled`/`refunded`になることを確認する。

実施日: ___ / 結果: ___ / 判定: 未実施

### ケース10: LINE送信失敗

LINEチャネルアクセストークンを一時的に空にした状態で権利付与を行い、
権利付与自体は成功すること、`line_notification_logs`に`status='failed'`の
行が記録されること、「LINE通知」欄の「再送」ボタンが機能することを確認する。

実施日: ___ / 結果: ___ / 判定: 未実施

### ケース11: 権限不足

本部担当者(operator)アカウントで、入金確認確定・ユーザー紐付け確定・
権利付与確定・取消/返金確定のAPIを直接叩き、いずれも403で拒否されることを
確認する。

実施日: ___ / 結果: ___ / 判定: 未実施

### ケース12: 既存Stripe直販

既存のStripe直販購入フロー(`/api/purchase/castle-plot-checkout`等)と
`/my-land`表示が、今回の変更によって壊れていないことを確認する
(コード上は触れていないが、`castle_plots`の列追加・`getExternalOrderDetail`
以外の共有ロジックへの影響がないか念のため確認)。

実施日: ___ / 結果: ___ / 判定: 未実施

### ケース13: 監査ログ

主要操作(注文登録・入金確認・紐付け・区画割当・権利付与・取消)を一通り
実行した後、`/admin/audit-logs`でそれぞれの操作者・操作日時・操作内容が
記録されていることを確認する。

実施日: ___ / 結果: ___ / 判定: 未実施

### ケース14: メタバース対応なし

`castle_plots.metaverse_property_id`が未設定の区画について、権利付与処理が
エラーにならず正常終了することを確認する(実装計画3-6・4章の「対応付けが
未設定でも権利付与処理が失敗しない」要件の確認。列を追加しただけで参照ロジック
自体は実装していないため、通常は問題なく通過するはずだが念のため確認する)。

実施日: ___ / 結果: ___ / 判定: 未実施

---

## 未解決事項

- 上記ケース1〜14は本番相当環境での手動実施が必要(このサンドボックスからは
  実施不可)。
- 一部取消(ケース9)は未実装。

## 本番適用時の注意事項

- `supabase/migrations/20260729000001_external_purchase_orders.sql` は
  CIでは自動適用されないため、本番Supabaseへの手動適用(`supabase db push`
  または SQL Editor)が必要(前回の城主プランマイグレーションと同様)。
- マイグレーション適用後、`external_orders`等の新規テーブルが実際に作成
  されているかをSupabaseダッシュボードで確認すること。
- LINE通知を実機で確認する場合は、`/admin/line-settings`でLINEチャネル
  アクセストークンが設定済みであることを確認すること。
